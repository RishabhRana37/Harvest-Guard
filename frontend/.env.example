# Harvest Guard Frontend Environment Variables
# Copy to .env.local or set in production deployment settings

# Base URL pointing to the live Harvest Guard FastAPI backend (without trailing /api/v1)
VITE_API_BASE_URL=https://<your-backend-host>
